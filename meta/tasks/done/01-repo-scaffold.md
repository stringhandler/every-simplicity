# Task: Repo Scaffold

Set up the base repository structure.

- `data/programs/` — directory for TOML entry files
- `site/` — static site source
- `cli/` — the `simplicity-catalog` local tool
- `.github/workflows/deploy.yml` — GitHub Actions build + Pages deploy
- `README.md` — basic project description and usage
