# Add word cloud to secondary tabs

On the Jets, Built-ins, Types, Macros, and Reserved Words tabs, add a word cloud visualization at the top of the tab showing all items sized by frequency (number of scripts using them).

Consider a pure CSS/SVG approach or a small JS word cloud library (e.g. d3-cloud or a lightweight alternative) to keep the bundle small.
